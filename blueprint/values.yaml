# Base configuration
Base:
  # GCP Project ID where the application will be deployed
  ProjectID: "progetto-1"

# WordPress application parameters
WordPress:
  # Namespace where the application will be deployed
  namespace: "namespacewordpress"

  # WordPress username
  username: "wordpress_user"

  # WordPress password (change this before deploying)
  password: "changeme"

  # URL of the WordPress application
  urlapplicativo: "inserireurlapplicativo"

  # WordPress version
  version: "25_0_0"
